// Auth module retired in v1.11.0 -- stdio transport does not require network
// authentication. Security is enforced at the tool level via the RED/AMBER/GREEN
// three-tier model in tools.ts.
export {};
